#include <iostream>

using namespace std;

int main() {
  for (char ch; cin.get(ch);) {
    if (isspace(ch)) {

    } else if (isdigit(ch)) {

    } else if (isalpha(ch)) {

    } else {

    }
  }
}
